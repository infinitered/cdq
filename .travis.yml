language: objective-c
osx_image: xcode8
before_install:
  - sudo motion update
  - motion repo
  - bundle install
  - bundle exec rake clean
script:
  - bundle exec rake spec
  - bundle exec rake spec platform=osx
